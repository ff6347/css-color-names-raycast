{
  "version": "1.3.1",
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "css-color-names",
  "title": "css color names",
  "description": "A list of CSS color names with to search",
  "icon": "icon.png",
  "author": "ff6347",
  "owner": "ff6347",
  "access": "private",
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "css-color-names-search",
      "title": "CSS Color Names Search",
      "description": "search the named css colors list",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.82.5",
    "@raycast/utils": "^1.16.3"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.8",
    "@technologiestiftung/semantic-release-config": "1.2.4",
    "@types/node": "20.8.10",
    "@types/react": "18.3.3",
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "semantic-release": "24.1.1",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "ray build -e dist -o dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
